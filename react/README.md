# react

Create the initial directory with the following command:

```sh
npx create-single-spa --moduleType app-parcel --framework react --skipInstall --typescript --packageManager npm --moduleFormat systemjs --dir react --orgName project --projectName react
```
